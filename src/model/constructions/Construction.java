package model.constructions;

import model.objets.Objet;
import model.objets.Position;

public class Construction extends Objet {
    public Construction(Position position, int rayon) {
        super(position, rayon);
    }
}
